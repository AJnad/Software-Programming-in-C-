#include <iostream>
#include <string>
using namespace std;

void getSize(int numSize)
{
    cout << "enter number of strings you want: ";
    cin >> numSize;
    cout << "so you want " << numSize << endl;
}

void getSpace (int numSize, string wordList)
{
    wordList = new wordList[numSize];
}

void inputData(int numSize, string wordList)
{
    for(int i = 0; i < numSize; i++)
    {
        cout <<
    }
}


int main()
{
    int numSize;
    string wordlist[];
    getSize(numSize);
}


